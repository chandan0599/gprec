# Python dependencies for backend/tools/*.py (see README.md > Running locally, PRODUCTION.md for
# a full deployment). portal_db_server.py talks to PostgreSQL by shelling out to the local `psql`
# binary, not a Python driver, so there's no psycopg2/psycopg entry here.
#
# Install with: pip3 install -r backend/requirements.txt
# pdf_render_server.py additionally needs a browser binary: playwright install chromium
# (on a bare Linux server: playwright install --with-deps chromium)

flask==3.1.3
cryptography==49.0.0
pypdf==6.14.2
python-docx==1.2.0
playwright==1.60.0

# WSGI server for static_server.py / pdf_render_server.py in production - `flask run` / `app.run()`
# (used by ./backend/tools/start_servers.sh) is a dev-only server, not meant to serve real traffic.
gunicorn==23.0.0
